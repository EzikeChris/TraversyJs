//// ARROW FUNCTIONS /// 

const figmaUser = (userName, userEmail, userHobby) => {
    return (
        'name is' + userName + 'email is ' + userEmail + 'this is hobby' + userHobby
    );

};

console.log(figmaUser());